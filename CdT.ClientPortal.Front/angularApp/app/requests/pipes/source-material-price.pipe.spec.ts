import { SourceMaterialPricePipe } from './source-material-price.pipe';

describe('SourceMaterialPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SourceMaterialPricePipe();
    expect(pipe).toBeTruthy();
  });
});
