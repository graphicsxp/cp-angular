import { SplitArrayToStringPipe } from './split-array-to-string.pipe';

describe('SplitArrayToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitArrayToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
