import { LanguageByRegionPipe } from './language-by-region.pipe';

describe('LanguageByRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageByRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
